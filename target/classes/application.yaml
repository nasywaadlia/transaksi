spring:
  application:
    name: transaksi

  profiles:
    active: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com
          
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
